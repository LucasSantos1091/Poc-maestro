# APK

Coloque aqui o APK do app a ser testado pelo Maestro, com o nome `app-debug.apk`.

O pipeline (`.github/workflows/maestro-tests.yml`) instala automaticamente o arquivo `apk/app-debug.apk` no emulador antes de rodar os fluxos.
